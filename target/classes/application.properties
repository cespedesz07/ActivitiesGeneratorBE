spring.jpa.hibernate.ddl-auto=update

# MySQL local DB
# spring.datasource.url=jdbc:mysql://localhost:3306/activities_generator_db
# spring.datasource.username=root
# spring.datasource.password=admin

# Heroku PostgreSQL
#spring.datasource.url:${JDBC_DATABASE_URL}
#spring.datasource.username:${JDBC_DATABASE_USERNAME}
#spring.datasource.password:${JDBC_DATABASE_PASSWORD}

# PostgreSQL local DB
spring.datasource.url=jdbc:postgresql://localhost:5432/activities_generator_psql


# -- EMAIL PROPERTIES --
# In order to enable the access to the gmail, this property should be enabled
# https://myaccount.google.com/lesssecureapps
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cespedesz07
spring.mail.password=$3rl0ck007*
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000